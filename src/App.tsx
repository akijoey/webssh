import React, { ReactElement } from 'react'

import Logo from '@/components/logo'

const App = (): ReactElement => {
  return (
    <div className="app">
      <Logo />
    </div>
  )
}

export default App
