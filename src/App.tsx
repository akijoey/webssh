import React, { ReactElement } from 'react'

import Home from '@/views/home'

const App = (): ReactElement => {
  return (
    <div className="app">
      <Home />
    </div>
  )
}

export default App
